# Notes — issues spotted outside the current task's scope

Logged here per the HD simulator briefs' instruction to record, not fix, anything outside scope.

- `practice.tsx` declared `fen`/`pgn`/`board_instructions` on its `Question` type but never rendered a board for them — **fixed in this pass** since it was small and independent of the bigger board rebuild (not "out of scope," just noting it here for the record).
- ~~Neither Blender nor KTX-Software (`toktx`) is installed~~ **Both installed 2026-09-20.** Blender 5.1.2 at `/Applications/Blender.app/Contents/MacOS/Blender`; KTX-Software v5.0.0-rc2 as the `ktx` CLI on PATH (note: v5 replaced the standalone `toktx` binary with `ktx create`/`ktx encode`). The procedural-geometry piece/board kit built in Phase 1 stays as-is (same props, no regression); real modeled/textured GLB assets are unblocked for a future visual upgrade and for Part A (the hall).
- Not yet verified: whether this sandbox can successfully download binary GLB files from Poly Haven/Sketchfab/Kenney/Quaternius over HTTPS — a known issue here causes large npm tarballs to fail TLS with `ERR_SSL_CIPHER_OPERATION_FAILED`. Untested for GLB-sized downloads specifically.
- ~~Practice session start appears broken~~ **Root-caused and resolved.** The transport-level 200 responses were masking a server-side exception: `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` weren't set in the deployment's environment, so every server function calling `supabaseAdmin` (practice, and a large chunk of admin/license/seminar/simulation functions) threw immediately. This was invisible for two independent reasons, both now fixed: (1) `<Toaster/>` was never mounted anywhere in the route tree, so no `toast()` call in the whole app rendered anything; (2) the practice `start` mutation had no `onError` handler, so even with a working Toaster nothing would have shown. The user has since added the missing secrets to the (now Vercel-hosted) deployment and redeployed; re-tested live with a temporary question and confirmed: session starts, questions render, the 3D board shows the new Staunton pieces, and stepping through a PGN animates the move correctly (pawn e2→e4, then e7→e5, both landed correctly with the from/to highlight matching).
- **Follow-up for later, not a bug:** Phase 4's board-exercise results (`ExerciseResult`) aren't yet folded into a simulation attempt's score. `completeAttempt`'s scoring only understands the existing multiple-choice `answers` log shape (`{step_id, choice_id, points, correct}`). A step with a `board_exercise` currently grades and displays correctly to the candidate but always contributes 0 points toward the attempt's final score/pass-fail. Fold this in when certificates/CPD wiring happens, per the brief's own explicit scope boundary for that work.
- ~~Pre-existing bug: unpublished scenarios never load, even for admins previewing a draft~~ **False alarm — not a bug.** Root-caused: this sandbox's Chrome browser session was logged in as `amisuomotoyosi2009@gmail.com` (a regular member, `national_arbiter` role, no `academy_user_roles` admin entry at all) -- a completely different account from the real super_admin (`amisuhikmot@gmail.com`, holds `academy_admin`/`super_admin`/`instructor`) used throughout this session's seed scripts. RLS was correctly denying that non-admin session access to unpublished scenarios the whole time; the earlier psql RLS simulation had used the *correct* super_admin's uid, which is why it looked like access should have been allowed and made the browser's denial look like a bug. Confirmed directly by querying the Supabase REST API from the browser's own session token: `academy_scenarios` returns `[]` for the unpublished scenario under that account, exactly as RLS intends.
  - **Smaller, real UX gap found along the way, fixed:** when `getScenarioForPlay`'s `.single()` legitimately gets zero rows (no access, or a bad id), the page used to hang on "Loading…" forever instead of showing an error -- same missing-error-handling shape as the earlier practice.tsx/Toaster issues. Added an error branch to `simulations.$scenarioId.tsx` so a denied/missing scenario now shows a clear message instead of spinning indefinitely.
- **Follow-up for later, not a bug:** since Part A's `table_kit_generate.py` swap to real classroom-sourced desk/chair meshes, the placeholder capsule "people" (`FigurePair` in `HallScene.tsx`, used on any table whose step has no chess `fen`) sit at a fixed `y=0.8` group offset calibrated for the old box-primitive table's flat top -- the real desk's top surface height differs slightly, so the capsules now read as floating a touch high relative to the desk. Purely cosmetic (verified live on `/dev/hall`), worth a small position tweak whenever Phase 4 (real characters) replaces `FigurePair` anyway rather than fixing twice.
- **Build-time-only dependency, not shippable:** `table_kit_generate.py` requires the source `classroom.blend` at a fixed local path on this machine (`/Users/abdulsemiuamisu/Downloads/classroom/classroom.blend`, user-provided, not committed to the repo). Regenerating `table-kit.glb` from scratch only works on this machine with that file present -- same category as needing Blender itself installed to run `hall:build`, just worth calling out since it's a new external-file dependency rather than a tool dependency.
- ~~Free walk mode: walking forward from spawn produced a solid black screen~~ **Root-caused and fixed.** `tableX()`'s fixed 5.5m-per-table spacing has no awareness of the hall shell's real, fixed 30x20m footprint -- `/dev/hall`'s 8-table QA demo spanned 38.5m, well outside the 30m-long room. This was invisible in guided-tour mode (the camera always frames just the active table up close, regardless of where the walls actually are) but broke Free walk mode outright: `HallWalkControls` spawned the camera at the out-of-bounds table position without clamping, so walking forward drove the camera into/through unintended geometry. Fixed two ways: `HallWalkControls` now clamps its *spawn* position through the same `resolveCollisions()` used for movement (defensive against any future out-of-bounds `startX`), and `/dev/hall`'s demo table count was reduced to 5 (fits inside the real hall's length). **Real remaining design tension, not fixed here:** `tableX()`'s spacing still has no relationship to the hall's fixed footprint, so a real scenario with enough steps would hit the same problem in guided-tour mode too (just less visibly, since collision isn't checked there). Phase 6 (seed content) will need to decide how many boards a 30x20m hall can actually hold and either cap step count or make the room scale with it.
